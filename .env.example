# Bản mẫu — giá trị thật điền vào .dev.vars (wrangler đọc file đó; .env là symlink cho quen tay)
SUPABASE_URL=https://<project-ref>.supabase.co
SUPABASE_SECRET_KEY=<service_role key — Dashboard → Settings → API Keys>
GEMINI_PROXY_KEY=<chỉ cần cho local dev>
